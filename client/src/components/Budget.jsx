//Here we will have where we set budget and a bar that tracks how far into it we are
import React from 'react'
import httpClient from '../httpClient';

function Budget() {
  return (
    <div>App</div>
  )
}

export default Budget
